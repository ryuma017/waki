services:
  database:
    container_name: waki-pg
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: waki
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
